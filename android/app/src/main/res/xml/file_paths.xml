<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Short-lived plaintext exports (PDF/JPEG/TXT) handed to the system
         share sheet via FileProvider URIs. Files self-delete after a few
         minutes — see SharePayloadBuilder.cleanupOld(). -->
    <cache-path name="shares" path="shares/" />
    <cache-path name="exports" path="exports/" />
</paths>
