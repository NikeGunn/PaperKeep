================================================================================
  SCANVAULT — COMPLETE PROMPT GUIDE (END-TO-END)
  From empty repo to live on Google Play Store

  HOW TO USE:
  1. Open Claude Code in C:\Users\Nautilus\Desktop\ScanVault
  2. Find today's session below
  3. Copy the PROMPT, paste into Claude Code
  4. Let it work
  5. When done, come back here, check [x] manually
  6. Tomorrow: next session

  TOTAL: 45 sessions across 11 weeks (~130 tasks)
  Each session = 1 prompt = 3-8 tasks = ~30-90 min of Claude work

  TEST-GATE RULE (applies to EVERY session):
  Claude MUST follow this sequence for each task:
    1. BUILD  → Write the implementation code
    2. TEST   → Write tests for every behavior
    3. RUN    → Execute ALL tests (new + existing)
    4. PASS   → ALL tests must pass. Fix failures before moving on.
    5. UPDATE → ONLY after all tests pass, check the box in PROGRESS.md
  If tests fail, DO NOT update PROGRESS.md. Fix the code first.
================================================================================


================================================================================
SESSION 01 — DevOps: Repo Skeleton
================================================================================
DATE: ___________  STATUS: [✅] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 0.1 through 0.6.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write validation tests (see below)
3. RUN all tests
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

For each task:
- 0.1: Create directories: android/, backend/, ota/, scripts/, docs/
  (intelligence/ already exists, keep it)
- 0.2: Create VERSION file containing exactly "0.1.0" (no newline)
- 0.3: Create root .gitignore covering Android (build/, .gradle/,
  *.apk, *.aab, local.properties), Go (bin/, vendor/), Python
  (__pycache__/, .venv/, *.pyc, models/*.bin), IDE (.idea/,
  .vscode/, *.iml), secrets (.env, *.jks, *.keystore,
  play-service-account.json), OS (.DS_Store, Thumbs.db)
- 0.4: Create root Makefile with targets that delegate: make android
  → cd android && ./gradlew, make backend → cd backend && make,
  make intelligence → cd intelligence && make, make test-all,
  make lint-all, make clean-all
- 0.5: Create .editorconfig (utf-8, lf line endings, 4-space indent
  for Kotlin/Java, tab for Go, 4-space for Python, trim trailing
  whitespace, insert final newline)
- 0.6: Create .pre-commit-config.yaml with commitlint hook enforcing
  conventional commits (types: feat,fix,perf,refactor,docs,test,
  build,ci,chore,revert; scopes: android,backend,intelligence,
  ci,docs,deps)

TESTS TO WRITE (scripts/test-phase0.sh):
- Verify all 6 directories exist
- Verify VERSION contains "0.1.0"
- Verify .gitignore has entries for each ecosystem (grep for key patterns)
- Verify Makefile has all targets (grep for target names)
- Verify .editorconfig has correct indent settings per language
- Verify .pre-commit-config.yaml is valid YAML and has commitlint

Run the test script. ALL checks must pass.
Only then update PROGRESS.md. Commit when all 6 done.

Read DEVOPS_AUTOMATION.md sections 1-4 for reference.
---

VERIFY AFTER:
  [ ] Test script scripts/test-phase0.sh passes all checks
  [ ] android/, backend/, ota/, scripts/, docs/ directories exist
  [ ] VERSION file contains "0.1.0"
  [ ] .gitignore covers all 5 ecosystems
  [ ] Makefile has delegation targets
  [ ] .editorconfig has correct indent per language
  [ ] .pre-commit-config.yaml has commitlint
  [ ] PROGRESS.md tasks 0.1-0.6 are checked ONLY after tests pass


================================================================================
SESSION 02 — DevOps: Scripts (Part 1)
================================================================================
DATE: ___________  STATUS: [✅] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 0.7 through 0.10.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write validation tests (see below)
3. RUN all tests
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

For each task:
- 0.7: Move FRONTEND_MVP.md, BACKEND_MVP.md, DESIGN_SYSTEM.md,
  DEVOPS_AUTOMATION.md, INTELLIGENCE_LAYER.md into docs/ folder.
  Keep CLAUDE.md, PROGRESS.md, prompt-guide.txt at root.
  Update any references in CLAUDE.md to point to docs/.
- 0.8: Create scripts/bootstrap.sh — idempotent dev setup script.
  Checks for: JDK 21, Android SDK/NDK, Go 1.23+, Docker, Python
  3.12+, pre-commit. Copies .env.example → .env if missing. Each
  check prints green checkmark or red X. Has --help flag.
- 0.9: Create scripts/doctor.sh — health check that reports status
  of all dev tools, Docker daemon, Postgres container, env vars.
  Has --help flag.
- 0.10: Create scripts/run-backend-local.sh — starts docker-compose
  (Postgres + Redis), waits healthy, runs goose migrations, starts
  Go API with air hot-reload, prints LAN IP + port. Has --help.

TESTS TO WRITE:
- 0.7: Verify all 5 spec files exist in docs/, none remain at root
- 0.8: Verify bootstrap.sh --help exits 0 and prints usage
- 0.8: Verify bootstrap.sh has set -euo pipefail
- 0.9: Verify doctor.sh --help exits 0 and prints usage
- 0.9: Verify doctor.sh has set -euo pipefail
- 0.10: Verify run-backend-local.sh --help exits 0 and prints usage
- 0.10: Verify run-backend-local.sh has set -euo pipefail
- All: Verify scripts are executable (chmod +x)

Run all tests. ALL must pass.
Only then update PROGRESS.md. Commit when all done.

Read DEVOPS_AUTOMATION.md section 2 for full spec of each script.
---

VERIFY AFTER:
  [ ] All tests pass
  [ ] docs/ contains all 5 spec files
  [ ] CLAUDE.md references updated to docs/ paths
  [ ] scripts/bootstrap.sh runs with --help
  [ ] scripts/doctor.sh runs with --help
  [ ] scripts/run-backend-local.sh runs with --help
  [ ] All scripts have set -euo pipefail
  [ ] PROGRESS.md tasks 0.7-0.10 are checked ONLY after tests pass


================================================================================
SESSION 03 — DevOps: Scripts (Part 2)
================================================================================
DATE: ___________  STATUS: [ ✅ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 0.11 through 0.13.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write validation tests (see below)
3. RUN all tests
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 0.11: Create scripts/run-phone.sh — builds debug APK, installs
  on connected device via ADB, launches main activity, streams
  filtered logcat. Flags: --wifi, --clean, --release, --profile,
  --backend <url>. Has --help.
- 0.12: Create scripts/release.sh — takes arg: patch/minor/major.
  Verifies clean main branch, pulls, runs tests, bumps VERSION,
  generates changelog from conventional commits, commits, tags
  vX.Y.Z, pushes with tags. Has --help. Asks for confirmation.
- 0.13: Create scripts/rollback.sh — takes arg: android/backend/
  ota/all. For backend: SSH to VPS, swap binary, restart systemd.
  For android: halt Play Store rollout. Has --help.

TESTS TO WRITE:
- 0.11: Verify --help exits 0, has all flags documented
- 0.12: Verify --help exits 0, accepts patch/minor/major args
- 0.12: Verify release.sh reads VERSION file correctly
- 0.13: Verify --help exits 0, accepts subcommands
- All: Verify set -euo pipefail, executable, shellcheck passes

Run all tests. ALL must pass.
Only then update PROGRESS.md. Check if Phase 0 is fully complete.
Commit when all done.

Read DEVOPS_AUTOMATION.md section 2 for full spec.
---

VERIFY AFTER:
  [ ] All tests pass
  [ ] scripts/run-phone.sh has --help and all flags
  [ ] scripts/release.sh has --help and confirmation prompt
  [ ] scripts/rollback.sh has --help and subcommands
  [ ] Phase 0 section in PROGRESS.md fully complete
  [ ] PROGRESS.md tasks 0.11-0.13 checked ONLY after tests pass


================================================================================
SESSION 04 — Backend: Go Project Scaffold
================================================================================
DATE: ___________  STATUS: [ ✅ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 1A.1 through 1A.4.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN: go test -race ./...
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 1A.1: Initialize Go module at backend/. Create full directory
  structure: cmd/api/main.go, internal/{config,auth,accounts,vault,
  sync,email,ratelimit,security,audit}/, db/{migrations,queries}/,
  deploy/{Caddyfile,scanvault.service}, scripts/rotate-keys.sh.
  go.mod with module name "github.com/nikhil/scanvault-api".
- 1A.2: Create backend/.golangci.yml — strict config enabling:
  errcheck, gosec, gocritic, revive, gofumpt, exportloopref,
  goconst, gosimple, ineffassign, staticcheck, typecheck, unused.
- 1A.3: Create backend/.env.example with all vars: DATABASE_URL,
  PASETO_KEY, ARGON2_TIME, ARGON2_MEMORY, ARGON2_THREADS,
  R2_ENDPOINT, R2_ACCESS_KEY, R2_SECRET_KEY, R2_BUCKET,
  POSTMARK_TOKEN, SENTRY_DSN, IP_HASH_KEY, SERVER_PORT,
  ENVIRONMENT (dev/staging/prod).
- 1A.4: Implement internal/config/ — struct with all env vars,
  loads via caarlos0/env/v10, validates required vars on startup,
  crashes with clear message if missing.

TESTS TO WRITE (in backend/internal/config/config_test.go):
- Test config loads correctly from env vars (set all vars, verify struct)
- Test config crashes on missing required vars (unset DATABASE_URL → panic)
- Test config defaults work (optional vars have sane defaults)
- Test environment validation (only dev/staging/prod accepted)

Run: cd backend && go test -race ./...
ALL tests must pass. Only then update PROGRESS.md. Commit.

Read BACKEND_MVP.md section 6 → Phase 1, deliverables 1-2 only.
---

VERIFY AFTER:
  [ ] go test -race ./... passes ALL tests
  [ ] Config tests cover: happy path, missing vars, defaults, validation
  [ ] backend/go.mod exists with correct module name
  [ ] All internal/ subdirectories exist
  [ ] .golangci.yml has strict rules
  [ ] .env.example has all documented vars
  [ ] PROGRESS.md tasks 1A.1-1A.4 checked ONLY after tests pass


================================================================================
SESSION 05 — Backend: Database & Migrations
================================================================================
DATE: ___________  STATUS: [✅] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 1A.5 through 1A.8.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN: go test -race ./...
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 1A.5: Create migration 0001 using goose. Tables: accounts (with
  id, uuid, email CITEXT, email_verified, auth_hash, auth_params
  JSONB, wrapped_key BYTEA, kdf_salt BYTEA, kdf_params JSONB,
  status with CHECK, timestamps), refresh_tokens (with token_hash
  BYTEA, device_label, last_used_at, expires_at, revoked_at),
  email_verification_tokens. All indexes as specified.
- 1A.6: Create sqlc.yaml config. Write SQL queries in
  db/queries/accounts.sql and db/queries/refresh_tokens.sql.
  Run sqlc generate.
- 1A.7: Implement cmd/api/main.go — chi router with middleware:
  middleware.RequestID, middleware.RealIP, custom slog logger,
  middleware.Recoverer, middleware.Timeout(60s), SecureHeaders,
  CORS. Graceful shutdown on SIGTERM.
- 1A.8: Implement /health (returns 200) and /ready (checks DB
  connection, returns component status).

TESTS TO WRITE:
- 1A.5: Integration test (testcontainers-go with Postgres): run
  migration up → verify tables exist → run migration down → verify
  tables gone. Test all constraints (insert invalid status → error).
- 1A.6: Verify sqlc generates without error. Test each query compiles.
- 1A.7: Test middleware chain order. Test graceful shutdown (send
  SIGTERM, verify in-flight requests complete). Test context cleanup.
- 1A.8: Test /health returns 200 with correct JSON body. Test /ready
  returns healthy when DB is up. Test /ready returns unhealthy when
  DB is down (stop container, check response).

Run: cd backend && go test -race ./...
ALL tests must pass. Only then update PROGRESS.md. Commit.

Read BACKEND_MVP.md section 3 (schema) and section 6 → Phase 1.
---

VERIFY AFTER:
  [ ] go test -race ./... passes ALL tests
  [ ] Migration tests verify table creation and constraints
  [ ] /health and /ready tested with real Postgres
  [ ] Migration file creates all 3 tables with correct columns
  [ ] sqlc generates Go code without errors
  [ ] PROGRESS.md tasks 1A.5-1A.8 checked ONLY after tests pass


================================================================================
SESSION 06 — Backend: Auth (Signup + Login)
================================================================================
DATE: ___________  STATUS: [ ✅ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 1A.9 through 1A.12.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN: go test -race ./...
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 1A.9: POST /v1/accounts — accept email, auth_hash, auth_params,
  wrapped_key, kdf_salt, kdf_params. Server hashes auth_hash AGAIN
  with Argon2id before storing. Validate email, check uniqueness,
  send verification email (stub for now), return 201 with UUID.
  Idempotent within 1-minute window.
- 1A.10: GET /v1/accounts/verify?token=... — validate token hash,
  mark account verified, one-time use, 24h expiry.
- 1A.11: POST /v1/sessions — Paseto v4, constant-time comparison.
  Dummy hash for non-existent users. Return access_token,
  refresh_token, crypto material.
- 1A.12: POST /v1/sessions/refresh — rotation + replay detection.
  If revoked token → revoke ALL tokens for account.

TESTS TO WRITE (integration tests with real Postgres):
- 1A.9: Happy path (valid input → 201, account in DB)
- 1A.9: Duplicate email → 409
- 1A.9: Missing required fields → 400
- 1A.9: Invalid email format → 400
- 1A.9: Idempotency (same request twice in 1 min → same UUID)
- 1A.10: Valid token → account verified
- 1A.10: Expired token → 410
- 1A.10: Already used token → 410
- 1A.10: Invalid token → 404
- 1A.11: Valid credentials → 200 with tokens
- 1A.11: Wrong password → 401
- 1A.11: Non-existent user → 401 (same timing as wrong password)
- 1A.11: Unverified account → 403
- 1A.12: Valid refresh → new token pair, old revoked
- 1A.12: Expired refresh → 401
- 1A.12: Revoked refresh → ALL account tokens revoked (replay detection)
- 1A.12: Concurrent refresh (race condition test)

Run: cd backend && go test -race ./...
ALL tests must pass. Only then update PROGRESS.md. Commit.

Read BACKEND_MVP.md section 4 (security) and section 6 → Phase 1.
---

VERIFY AFTER:
  [ ] go test -race ./... passes ALL tests (17+ test cases)
  [ ] Auth tests cover: happy path, errors, edge cases, concurrency
  [ ] Same response time for valid/invalid users (timing test)
  [ ] Replay detection revokes all sessions
  [ ] PROGRESS.md tasks 1A.9-1A.12 checked ONLY after tests pass


================================================================================
SESSION 07 — Backend: Auth Completion
================================================================================
DATE: ___________  STATUS: [ ✅ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 1A.13 through 1A.18.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN: go test -race ./...
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 1A.13: DELETE /v1/sessions — revoke current refresh token.
- 1A.14: GET /v1/accounts/me — requires auth, returns account info.
- 1A.15: POST /v1/accounts/me/password — verify old, update new,
  revoke all refresh tokens atomically.
- 1A.16: Auth middleware — extract Bearer, verify Paseto, load account.
- 1A.17: Rate limiting — in-memory. Login: 5/IP/15min. Signup: 3/IP/hr.
- 1A.18: Security headers — nosniff, DENY, no-referrer, CSP.

TESTS TO WRITE:
- 1A.13: Logout → refresh token no longer works
- 1A.13: Logout without auth → 401
- 1A.14: GET /me with valid token → 200 with correct fields
- 1A.14: GET /me without auth → 401
- 1A.14: GET /me with expired token → 401
- 1A.14: Response NEVER contains auth_hash or secret material
- 1A.15: Change password → old password stops working, new works
- 1A.15: Change password → all existing sessions revoked
- 1A.15: Wrong current password → 401
- 1A.16: Valid Paseto → account loaded in context
- 1A.16: Expired Paseto → 401
- 1A.16: Suspended account → 403
- 1A.16: Missing Bearer → 401
- 1A.17: 6th login attempt in 15 min → 429
- 1A.17: 4th signup attempt in 1 hour → 429
- 1A.17: Rate limit resets after window expires
- 1A.18: All security headers present on every response
- 1A.18: CSP blocks inline scripts (verify header content)

Run: cd backend && go test -race ./...
ALL tests must pass. Only then update PROGRESS.md. Commit.

Read BACKEND_MVP.md section 6 → Phase 1 deliverables 9-14.
---

VERIFY AFTER:
  [ ] go test -race ./... passes ALL tests (18+ test cases)
  [ ] Rate limiter tested with threshold + reset
  [ ] Security headers verified on responses
  [ ] Auth middleware tested with all token states
  [ ] PROGRESS.md tasks 1A.13-1A.18 checked ONLY after tests pass


================================================================================
SESSION 08 — Backend: Logging, Docker, Makefile
================================================================================
DATE: ___________  STATUS: [ ✅ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 1A.19 through 1A.22.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN: go test -race ./...
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 1A.19: Configure slog JSON handler. Every request logged with
  request_id, method, path, status, duration_ms. NEVER log
  auth_hash, wrapped_key, or any secret field. Add slog attributes
  for version, commit, build_date.
- 1A.20: Write unit tests for internal/auth (Argon2id hashing,
  Paseto generation/verification, token rotation). Integration
  tests for all endpoints using real Postgres via testcontainers-go.
  Target 80% coverage for auth + accounts.
- 1A.21: Create backend/docker-compose.yml with Postgres 16 service.
- 1A.22: Create backend/Makefile with targets: run, test, lint,
  migrate-up, migrate-down, sqlc, build.

TESTS TO WRITE:
- 1A.19: Test log output is valid JSON
- 1A.19: Test request_id, method, path, status, duration_ms present
- 1A.19: Test auth_hash/wrapped_key NEVER appear in log output
- 1A.19: Test version/commit/build_date in log attributes
- 1A.20: Full test suite achieving 80%+ coverage (run with -cover)
- 1A.21: Test docker-compose config is valid YAML
- 1A.21: Test Postgres starts and accepts connections
- 1A.22: Verify Makefile has all required targets (parse for target names)

Run: cd backend && go test -race -cover ./...
Coverage must be >= 80% for auth + accounts packages.
ALL tests must pass. Only then update PROGRESS.md. Commit.
---

VERIFY AFTER:
  [ ] go test -race -cover ./... passes, coverage >= 80%
  [ ] Logs are JSON, no secrets leaked (verified by test)
  [ ] docker-compose up starts Postgres
  [ ] make run / make test / make lint all work
  [ ] PROGRESS.md tasks 1A.19-1A.22 checked ONLY after tests pass


================================================================================
SESSION 09 — Backend: Deploy to Staging
================================================================================
DATE: ___________  STATUS: [✅ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete task 1A.23.

FOLLOW THE TEST-GATE RULE:
1. BUILD the deployment configs
2. TEST — validate configs (see below)
3. RUN validation
4. PASS — all checks pass
5. UPDATE PROGRESS.md only after validation passes

- 1A.23: Create deployment config for staging VPS:
  - deploy/Caddyfile: reverse proxy api.scanvault.app → :8080,
    automatic HTTPS, TLS 1.3 minimum, HSTS with preload.
  - deploy/scanvault.service: systemd unit running the Go binary,
    restart=on-failure, EnvironmentFile with 0600 perms.
  - Document manual steps in docs/DEPLOY.md.

TESTS TO WRITE:
- Verify Caddyfile has correct domain and port
- Verify Caddyfile has TLS 1.3 minimum and HSTS
- Verify systemd service file has correct [Unit], [Service], [Install]
- Verify systemd has restart=on-failure
- Verify EnvironmentFile referenced (not hardcoded secrets)
- Verify docs/DEPLOY.md exists and has key sections

ALL checks must pass. Only then update PROGRESS.md.
Check if Phase 1A is fully complete.
Commit: "feat(backend): complete Phase 1 scaffold with full tests"
---

VERIFY AFTER:
  [ ] All validation checks pass
  [ ] deploy/Caddyfile has correct config
  [ ] deploy/scanvault.service is valid
  [ ] docs/DEPLOY.md has step-by-step VPS setup
  [ ] ALL Phase 1A tasks in PROGRESS.md are checked
  [ ] PROGRESS.md tasks checked ONLY after tests pass


================================================================================
SESSION 10 — Frontend: Android Project Scaffold
================================================================================
DATE: ___________  STATUS: [ ✅ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 1B.1 through 1B.4.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN: cd android && ./gradlew testDebugUnitTest
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 1B.1: Create multi-module Android project at android/. Modules:
  :app, :core:ui, :core:common, :core:data, :core:domain, :core:ml,
  :core:imaging, :core:pdf, :core:network, :feature:scanner,
  :feature:library, :feature:reader, :feature:settings.
  Min SDK 26, target SDK 35, Kotlin 2.0+, Compose BOM.
- 1B.2: Create gradle/libs.versions.toml with all dependencies.
- 1B.3: Create detekt.yml and proguard-rules.pro.
- 1B.4: Wire up Hilt — @HiltAndroidApp, @AndroidEntryPoint, basic module.

TESTS TO WRITE:
- 1B.1: Test project compiles (./gradlew assembleDebug succeeds)
- 1B.1: Test all modules are recognized (./gradlew projects lists them)
- 1B.4: Test Hilt injection works — write a simple unit test that
  verifies Application context is injected correctly
- 1B.4: Test MainActivity launches without crash (Compose UI test)

Run: cd android && ./gradlew testDebugUnitTest
ALL tests must pass. Only then update PROGRESS.md. Commit.

Read FRONTEND_MVP.md section 2 and DESIGN_SYSTEM.md section 2.2.
---

VERIFY AFTER:
  [ ] ./gradlew testDebugUnitTest passes
  [ ] ./gradlew projects shows all modules
  [ ] Hilt injection test passes
  [ ] libs.versions.toml has all dependencies
  [ ] PROGRESS.md tasks 1B.1-1B.4 checked ONLY after tests pass


================================================================================
SESSION 11 — Frontend: Theme + Splash + Icon
================================================================================
DATE: ___________  STATUS: [✅ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 1B.5 through 1B.7.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN: cd android && ./gradlew testDebugUnitTest
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 1B.5: Material 3 theme with dynamic color. Colors from
  DESIGN_SYSTEM.md section 2.2. Light + dark color schemes.
- 1B.6: Splash screen API — SplashScreen compat library,
  brand animation, 300ms max.
- 1B.7: Adaptive app icon placeholder.

TESTS TO WRITE:
- 1B.5: Compose UI test — theme provides correct colors in light mode
- 1B.5: Compose UI test — theme provides correct colors in dark mode
- 1B.5: Test dynamic color fallback on devices without support
- 1B.6: Test splash screen displays and dismisses
- 1B.7: Test icon resources exist (foreground + background layers)

Run: cd android && ./gradlew testDebugUnitTest
ALL tests must pass. Only then update PROGRESS.md. Commit.

Read DESIGN_SYSTEM.md sections 2.2-2.3.
---

VERIFY AFTER:
  [ ] ./gradlew testDebugUnitTest passes
  [ ] Theme tests verify light and dark colors
  [ ] Splash screen shows and dismisses correctly
  [ ] PROGRESS.md tasks 1B.5-1B.7 checked ONLY after tests pass


================================================================================
SESSION 12 — Frontend: Camera Permissions
================================================================================
DATE: ___________  STATUS: [ ✅] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 1B.8 through 1B.9.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN tests
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 1B.8: Camera permissions flow — rationale screen, denial state
  with "Open Settings" button, handle "Don't ask again".
- 1B.9: CameraX PreviewView (4:3 aspect ratio, safe-area insets).

TESTS TO WRITE:
- 1B.8: UI test — permission rationale screen renders
- 1B.8: UI test — "Open Settings" button visible on permanent denial
- 1B.8: UI test — camera screen shows when permission granted
- 1B.8: Test back button from rationale screen
- 1B.9: UI test — preview view renders without crash
- 1B.9: Test aspect ratio is 4:3

Run tests. ALL must pass.
Only then update PROGRESS.md. Commit.

Read FRONTEND_MVP.md section 4 → Phase 1.
---

VERIFY AFTER:
  [ ] All tests pass
  [ ] Permission flow handles grant, deny, permanent deny
  [ ] Camera preview renders
  [ ] PROGRESS.md tasks 1B.8-1B.9 checked ONLY after tests pass


================================================================================
SESSION 13 — Frontend: Camera Controls
================================================================================
DATE: ___________  STATUS: [ ✅] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete task 1B.10.

FOLLOW THE TEST-GATE RULE:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN tests
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 1B.10: Camera controls — torch toggle, grid overlay (3x3),
  capture button with haptic feedback, pinch-to-zoom, tap-to-focus
  with ring animation.

TESTS TO WRITE:
- UI test: torch toggle button renders and toggles state
- UI test: grid overlay renders 4 lines (2 horizontal, 2 vertical)
- UI test: capture button renders and responds to click
- UI test: zoom gesture changes zoom level
- UI test: tap-to-focus shows ring animation
- Test: all touch targets >= 48dp
- Test: contentDescription on all interactive elements

Run tests. ALL must pass.
Only then update PROGRESS.md. Commit.
---

VERIFY AFTER:
  [ ] All tests pass
  [ ] Camera controls work (torch, grid, capture, zoom, focus)
  [ ] Accessibility: contentDescription + 48dp targets
  [ ] PROGRESS.md task 1B.10 checked ONLY after tests pass


================================================================================
SESSION 14 — Frontend: Edge Detection
================================================================================
DATE: ___________  STATUS: [✅ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 1B.11 through 1B.12.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN tests
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 1B.11: Real-time edge detection (OpenCV: grayscale → blur → Canny
  → contours → corners). Run on background thread, 30fps target.
- 1B.12: Edge detection overlay on Compose Canvas (green when good
  detection, amber when partial, invisible when none, spring animation).

TESTS TO WRITE:
- 1B.11: Unit test — edge detector finds rectangle in synthetic image
- 1B.11: Unit test — edge detector returns empty for uniform image
- 1B.11: Test processing doesn't block main thread
- 1B.11: Test handles null/empty bitmap gracefully
- 1B.12: UI test — overlay renders green when corners detected
- 1B.12: UI test — overlay renders amber when partial detection
- 1B.12: UI test — overlay invisible when no detection
- 1B.12: Test animation transitions between states

Run tests. ALL must pass.
Only then update PROGRESS.md. Commit.
---

VERIFY AFTER:
  [ ] All tests pass
  [ ] Edge detection works on synthetic test images
  [ ] Overlay renders correctly in 3 states
  [ ] PROGRESS.md tasks 1B.11-1B.12 checked ONLY after tests pass


================================================================================
SESSION 15 — Frontend: Capture + Crop
================================================================================
DATE: ___________  STATUS: [ ✅] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 1B.13 through 1B.14.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN tests
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 1B.13: Capture pipeline — full-res capture → edge detection →
  perspective transform → save.
- 1B.14: Manual crop screen — 4 draggable corners, magnifier on
  drag, rotate 90°, retake/next buttons.

TESTS TO WRITE:
- 1B.13: Unit test — perspective transform produces correct output
- 1B.13: Test capture pipeline handles large image (4000x3000)
- 1B.13: Test pipeline doesn't crash on corrupted image data
- 1B.14: UI test — crop screen renders with 4 corner handles
- 1B.14: UI test — corners are draggable (simulate drag gesture)
- 1B.14: UI test — rotate button rotates image 90°
- 1B.14: UI test — retake button navigates back
- 1B.14: UI test — next button proceeds to save
- 1B.14: Test state survives rotation (rememberSaveable)

Run tests. ALL must pass.
Only then update PROGRESS.md. Commit.
---

VERIFY AFTER:
  [ ] All tests pass
  [ ] Capture pipeline tested with real image data
  [ ] Crop screen tested with drag gestures
  [ ] State survives rotation
  [ ] PROGRESS.md tasks 1B.13-1B.14 checked ONLY after tests pass


================================================================================
SESSION 16 — Frontend: Storage + Room + Navigation
================================================================================
DATE: ___________  STATUS: [✅ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 1B.15 through 1B.20.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN: cd android && ./gradlew testDebugUnitTest
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 1B.15: Encrypted storage (AES-256-GCM, Android Keystore master key).
- 1B.16: Room ScanEntity + DAO.
- 1B.17: Recent scans thumbnail strip on camera screen.
- 1B.18: Navigation Compose with type-safe routes.
- 1B.19: Screen rotation support (rememberSaveable + ViewModel).
- 1B.20: Unit tests + Compose UI tests for all critical flows.

TESTS TO WRITE:
- 1B.15: Test round-trip (encrypt → decrypt → same data)
- 1B.15: Test encrypted file is not readable as plaintext
- 1B.15: Test encryption handles empty data
- 1B.15: Test encryption handles large data (10MB)
- 1B.15: Test concurrent encrypt/decrypt operations
- 1B.16: Test ScanEntity insert → query → same data
- 1B.16: Test DAO returns empty list when no scans
- 1B.16: Test DAO delete removes entity
- 1B.16: Test DAO handles unicode in title
- 1B.17: UI test — thumbnail strip renders with scans
- 1B.17: UI test — thumbnail strip shows empty state
- 1B.18: Test navigation between camera → crop → library
- 1B.18: Test deep link handling
- 1B.19: Test state persists after config change
- 1B.20: Comprehensive UI test covering full scan flow

Run: cd android && ./gradlew testDebugUnitTest
ALL tests must pass. Only then update PROGRESS.md.
Check if Phase 1B is fully complete.
Commit: "feat(android): complete Phase 1 with full test coverage"
---

VERIFY AFTER:
  [ ] ./gradlew testDebugUnitTest passes ALL tests
  [ ] Encryption round-trip tested
  [ ] Room DAO tested
  [ ] Navigation tested
  [ ] ALL Phase 1B tasks in PROGRESS.md are checked
  [ ] PROGRESS.md tasks checked ONLY after tests pass


================================================================================
SESSION 17 — CI/CD Pipelines
================================================================================
DATE: ___________  STATUS: [ ✅ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 1C.1 through 1C.5.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the workflow YAML
2. TEST — validate (see below)
3. RUN validation
4. PASS — all checks pass
5. UPDATE PROGRESS.md only after validation passes

- 1C.1: android-ci.yml (lint → test → assemble → upload artifact)
- 1C.2: backend-ci.yml (vet → staticcheck → gosec → govulncheck → test)
- 1C.3: Verify intelligence-ci.yml works. Fix any issues.
- 1C.4: backend-deploy-staging.yml (auto-deploy on merge to main)
- 1C.5: security-scan.yml (weekly OWASP + govulncheck + trufflehog)

TESTS TO WRITE (CI/CD validation):
- Verify each YAML file is valid YAML (no tabs, correct indentation)
- Verify concurrency groups set to cancel-in-progress
- Verify all secrets are referenced via ${{ secrets.* }}, NEVER hardcoded
- Verify correct trigger conditions (on: push/pull_request with paths)
- Verify timeout-minutes set on every job
- Verify actions versions pinned (actions/checkout@v4, not @latest)
- Verify no hardcoded credentials in env blocks
- Verify security-scan.yml has cron schedule

Write a validation script that checks all the above.
Run it. ALL checks must pass.
Only then update PROGRESS.md. Commit.

Read DEVOPS_AUTOMATION.md section 6 for workflow specs.
---

VERIFY AFTER:
  [ ] Validation script passes all checks
  [ ] All 5 workflow files created and valid
  [ ] Secrets referenced, not hardcoded
  [ ] Concurrency groups and timeouts set
  [ ] PROGRESS.md tasks 1C.1-1C.5 checked ONLY after tests pass


================================================================================
SESSION 18 — Backend Phase 2: R2 + Vault Schema
================================================================================
DATE: ___________  STATUS: [ ✅ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 2A.1 through 2A.4.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN: go test -race ./...
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 2A.1: R2 storage wrapper (presigned URLs, CRUD, 5-min expiry).
- 2A.2: Migration 0002 (vault_documents, vault_pages, account_quotas).
- 2A.3: POST /v1/vault/documents — create with client UUID v7.
- 2A.4: GET /v1/vault/documents (cursor pagination) and GET by UUID.

TESTS TO WRITE:
- 2A.1: Test presigned URL generation (mock S3 client)
- 2A.1: Test key format matches vault/{account}/{doc}/{page}.enc
- 2A.1: Test URL expiry is 5 minutes
- 2A.2: Integration test — migration up creates tables, down drops them
- 2A.2: Test constraints (foreign keys, NOT NULL, CHECK)
- 2A.3: Happy path — valid create → 201 with UUID
- 2A.3: Missing required fields → 400
- 2A.3: No auth → 401
- 2A.3: Duplicate UUID → 409
- 2A.4: List returns documents sorted, cursor pagination works
- 2A.4: List excludes soft-deleted documents
- 2A.4: Get by UUID → correct document
- 2A.4: Get other user's document → 404 (not 403)
- 2A.4: Get non-existent UUID → 404

Run: cd backend && go test -race ./...
ALL tests must pass. Only then update PROGRESS.md. Commit.

Read BACKEND_MVP.md section 6 → Phase 2, deliverables 1-5.
---

VERIFY AFTER:
  [ ] go test -race ./... passes ALL tests (14+ cases)
  [ ] R2 wrapper tested with mock
  [ ] Vault endpoints tested: happy path, auth, errors, authorization
  [ ] Cursor pagination tested
  [ ] PROGRESS.md tasks 2A.1-2A.4 checked ONLY after tests pass


================================================================================
SESSION 19 — Backend Phase 2: Upload/Download + Sync
================================================================================
DATE: ___________  STATUS: [ ✅ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 2A.5 through 2A.12.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN: go test -race ./...
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 2A.5: PUT document — optimistic concurrency via expected_version.
- 2A.6: DELETE document — soft delete.
- 2A.7: Page upload flow (request URL → upload → confirm).
- 2A.8: Page download URL endpoint.
- 2A.9: Background purge goroutine (hourly, >30 days soft-deleted).
- 2A.10: Quota enforcement (500MB / 1000 docs default).
- 2A.11: GET /v1/vault/manifest?since=<timestamp>.
- 2A.12: Full integration test.

TESTS TO WRITE:
- 2A.5: Optimistic concurrency — correct version → 200, wrong → 409
- 2A.5: Concurrent updates → only one succeeds
- 2A.6: Soft delete → document gone from list but in DB
- 2A.6: Delete other user's doc → 404
- 2A.7: Upload flow → pending → confirmed (full lifecycle)
- 2A.7: Upload exceeding 50MB → 413
- 2A.7: Upload over quota → 402
- 2A.8: Download URL returns presigned GET URL
- 2A.8: Download other user's page → 404
- 2A.9: Purge deletes >30 day soft-deleted documents
- 2A.9: Purge doesn't touch <30 day soft-deleted
- 2A.10: Upload at quota limit → 402
- 2A.10: Quota resets after deletion
- 2A.11: Manifest returns only docs changed since timestamp
- 2A.11: Manifest has cursor pagination
- 2A.12: Full E2E test: create → upload → confirm → list → download → delete → purge

Run: cd backend && go test -race ./...
ALL tests must pass. Only then update PROGRESS.md.
Check if Phase 2A is fully complete.
Commit: "feat(backend): Phase 2 vault & upload with full tests"
---

VERIFY AFTER:
  [ ] go test -race ./... passes ALL tests (17+ cases)
  [ ] Optimistic concurrency tested with race conditions
  [ ] Upload/download lifecycle fully tested
  [ ] Purge and quota tested
  [ ] ALL Phase 2A tasks in PROGRESS.md are checked
  [ ] PROGRESS.md tasks checked ONLY after tests pass


================================================================================
SESSION 20 — Frontend Phase 2: Room Entities + Library Screen
================================================================================
DATE: ___________  STATUS: [✅ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 2B.1 through 2B.3.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN: cd android && ./gradlew testDebugUnitTest
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 2B.1: Document + Page Room entities with relations.
- 2B.2: Library screen (grid, cards, multi-select, sort).
- 2B.3: Folders (one level deep, create/rename/delete).

TESTS TO WRITE:
- 2B.1: Test Document entity insert → query → correct data
- 2B.1: Test Page entity with foreign key to Document
- 2B.1: Test cascade delete (delete Document → Pages gone)
- 2B.1: Test entity with unicode title
- 2B.2: UI test — library renders grid of document cards
- 2B.2: UI test — empty state shown when no documents
- 2B.2: UI test — multi-select mode (long press activates)
- 2B.2: UI test — sort by date, name, size
- 2B.3: Test create folder → appears in list
- 2B.3: Test rename folder
- 2B.3: Test delete folder (documents moved to root)
- 2B.3: Test folder depth is exactly 1 (no nested folders)

Run: cd android && ./gradlew testDebugUnitTest
ALL tests must pass. Only then update PROGRESS.md. Commit.

Read FRONTEND_MVP.md section 4 → Phase 2 and DESIGN_SYSTEM.md 2.7.
---

VERIFY AFTER:
  [ ] All tests pass (12+ cases)
  [ ] Room entities tested with real database
  [ ] Library UI tested: grid, empty, multi-select, sort
  [ ] Folder operations tested
  [ ] PROGRESS.md tasks 2B.1-2B.3 checked ONLY after tests pass


================================================================================
SESSION 21 — Frontend Phase 2: Search + Multi-page
================================================================================
DATE: ___________  STATUS: [✅ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 2B.4 through 2B.6.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN tests
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 2B.4: Full-text search (Room FTS4).
- 2B.5: Multi-page capture flow (batch mode, reorder screen).
- 2B.6: Image filters (Original, Auto, Magic Color, Grayscale, B&W).

TESTS TO WRITE:
- 2B.4: Test FTS search finds document by title
- 2B.4: Test FTS search finds document by OCR text content
- 2B.4: Test search returns empty for no matches
- 2B.4: Test search handles special characters safely
- 2B.5: UI test — batch capture adds multiple pages
- 2B.5: UI test — reorder screen allows drag to reorder
- 2B.5: UI test — delete page from batch
- 2B.5: Test state survives rotation during batch
- 2B.6: Unit test — each filter produces different output
- 2B.6: Unit test — Original filter returns unmodified image
- 2B.6: UI test — filter preview thumbnails render

Run tests. ALL must pass.
Only then update PROGRESS.md. Commit.
---

VERIFY AFTER:
  [ ] All tests pass
  [ ] FTS search works with title and content
  [ ] Multi-page flow tested
  [ ] All 5 filters produce correct output
  [ ] PROGRESS.md tasks 2B.4-2B.6 checked ONLY after tests pass


================================================================================
SESSION 22 — Frontend Phase 2: OCR + PDF + Exports
================================================================================
DATE: ___________  STATUS: [ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 2B.7 through 2B.9.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN tests
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 2B.7: OCR pipeline (ML Kit v2, Latin bundled, on-device).
- 2B.8: PDF export (PdfDocument + PDFBox text layer).
- 2B.9: Other exports (JPEG, PNG, TXT, encrypted ZIP).

TESTS TO WRITE:
- 2B.7: Test OCR extracts text from synthetic image with clear text
- 2B.7: Test OCR returns empty string for image without text
- 2B.7: Test OCR handles corrupted bitmap gracefully
- 2B.8: Test PDF export creates valid PDF file
- 2B.8: Test PDF has searchable text layer (not just image)
- 2B.8: Test multi-page PDF (3 pages → 3-page PDF)
- 2B.8: Test PDF export with no pages → error handled
- 2B.9: Test JPEG export produces valid JPEG
- 2B.9: Test PNG export produces valid PNG
- 2B.9: Test TXT export contains OCR text
- 2B.9: Test encrypted ZIP requires password to open
- 2B.9: Test encrypted ZIP with wrong password fails

Run tests. ALL must pass.
Only then update PROGRESS.md. Commit.
---

VERIFY AFTER:
  [ ] All tests pass (12+ cases)
  [ ] OCR tested with synthetic images
  [ ] PDF export creates valid searchable PDFs
  [ ] All export formats tested
  [ ] PROGRESS.md tasks 2B.7-2B.9 checked ONLY after tests pass


================================================================================
SESSION 23 — Frontend Phase 2: Reader + Biometric + Integration
================================================================================
DATE: ___________  STATUS: [ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 2B.10 through 2B.12.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN tests
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 2B.10: Document reader (swipeable pager, pinch-to-zoom, OCR text
  overlay, FLAG_SECURE).
- 2B.11: Biometric app lock.
- 2B.12: Integration test (10-page scan → reorder → filter → export).

TESTS TO WRITE:
- 2B.10: UI test — reader renders document pages
- 2B.10: UI test — swipe navigates between pages
- 2B.10: UI test — pinch-to-zoom works
- 2B.10: UI test — OCR text overlay toggles
- 2B.10: Test FLAG_SECURE is set (screenshot blocked)
- 2B.11: Test biometric prompt shows when app lock enabled
- 2B.11: Test app accessible when biometric disabled
- 2B.11: Test fallback when biometric hardware unavailable
- 2B.12: Full integration test — 10 pages through complete pipeline:
  capture → reorder → apply filter → export PDF → verify output

Run tests. ALL must pass.
Only then update PROGRESS.md.
Check if Phase 2B is fully complete.
Commit: "feat(android): Phase 2 library & OCR with full tests"
---

VERIFY AFTER:
  [ ] All tests pass
  [ ] Reader tested: swipe, zoom, overlay, secure flag
  [ ] Biometric lock tested
  [ ] Integration test covers full pipeline
  [ ] ALL Phase 2B tasks in PROGRESS.md are checked
  [ ] PROGRESS.md tasks checked ONLY after tests pass


================================================================================
SESSION 24 — Backend Phase 3: Sync + Security (Part 1)
================================================================================
DATE: ___________  STATUS: [ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 3A.1 through 3A.5.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN: go test -race ./...
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 3A.1: Conflict resolution protocol + conflict backup table (migration 0003).
- 3A.2: Batch operations endpoint.
- 3A.3: Redis rate limiter (replace in-memory).
- 3A.4: CAPTCHA on signup (hCaptcha/Turnstile).
- 3A.5: Account lockout after brute force.

TESTS TO WRITE:
- 3A.1: Test conflict detected when versions diverge
- 3A.1: Test server wins by default, client copy backed up
- 3A.1: Test conflict backup retrievable
- 3A.2: Test batch create/update/delete in single request
- 3A.2: Test batch with one invalid item → partial success
- 3A.2: Test batch over limit → 400
- 3A.3: Test Redis rate limiter counts correctly
- 3A.3: Test rate limit resets after window
- 3A.3: Test rate limit works across multiple requests
- 3A.4: Test signup without CAPTCHA → 400
- 3A.4: Test signup with invalid CAPTCHA → 400
- 3A.4: Test signup with valid CAPTCHA → success
- 3A.5: Test 5 failed logins → account locked
- 3A.5: Test locked account cannot login
- 3A.5: Test lockout expires after cooldown period

Run: cd backend && go test -race ./...
ALL tests must pass. Only then update PROGRESS.md. Commit.

Read BACKEND_MVP.md section 6 → Phase 3.
---

VERIFY AFTER:
  [ ] go test -race ./... passes ALL tests (15+ cases)
  [ ] Conflict resolution tested
  [ ] Redis rate limiter tested (needs Redis in docker-compose)
  [ ] CAPTCHA and lockout tested
  [ ] PROGRESS.md tasks 3A.1-3A.5 checked ONLY after tests pass


================================================================================
SESSION 25 — Backend Phase 3: Security + Account Management
================================================================================
DATE: ___________  STATUS: [ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 3A.6 through 3A.11.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN: go test -race ./...
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 3A.6: Password reset flow (with data loss warning).
- 3A.7: Audit event logging (all security events).
- 3A.8: Account activity endpoint.
- 3A.9: Session listing & revocation.
- 3A.10: Delete account flow (7-day grace + hard purge).
- 3A.11: Abuse signals.

TESTS TO WRITE:
- 3A.6: Test password reset email sent (mock)
- 3A.6: Test reset token works within expiry
- 3A.6: Test expired reset token → error
- 3A.6: Test data loss warning included in response
- 3A.7: Test security events logged (login, password change, etc.)
- 3A.7: Test audit log queryable by account
- 3A.8: Test activity endpoint returns recent events
- 3A.8: Test activity endpoint requires auth
- 3A.9: Test list sessions returns active sessions
- 3A.9: Test revoke specific session
- 3A.9: Test revoke other user's session → 404
- 3A.10: Test delete request starts 7-day grace period
- 3A.10: Test cancel delete within grace period
- 3A.10: Test hard purge after 7 days (everything gone)
- 3A.10: Test deleted account cannot login
- 3A.11: Test abuse signal recorded

Run: cd backend && go test -race ./...
ALL tests must pass. Only then update PROGRESS.md.
Check if Phase 3A is fully complete.
Commit: "feat(backend): Phase 3 sync & security with full tests"

Read BACKEND_MVP.md section 6 → Phase 3.
---

VERIFY AFTER:
  [ ] go test -race ./... passes ALL tests (16+ cases)
  [ ] Password reset flow fully tested
  [ ] Account deletion grace period tested
  [ ] ALL Phase 3A tasks in PROGRESS.md are checked
  [ ] PROGRESS.md tasks checked ONLY after tests pass


================================================================================
SESSION 26 — Frontend Phase 3: Smart Modes (Part 1)
================================================================================
DATE: ___________  STATUS: [ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 3B.1 through 3B.5.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN tests
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 3B.1: Smart document type detection (TFLite classifier).
- 3B.2: ID card mode (front + back → single A4).
- 3B.3: Receipt mode (tall aspect, B&W, field extraction).
- 3B.4: Whiteboard mode (glare removal, marker boost).
- 3B.5: Book scan mode (two-page split, DewarpNet).

TESTS TO WRITE:
- 3B.1: Test classifier detects document, receipt, ID, whiteboard
- 3B.1: Test classifier returns confidence scores
- 3B.1: Test classifier handles non-document image gracefully
- 3B.2: UI test — ID card prompts for front and back
- 3B.2: Test composite output is A4 format
- 3B.3: Test receipt mode uses tall aspect ratio
- 3B.3: Test receipt field extraction (total, merchant)
- 3B.4: Test whiteboard mode removes glare (compare before/after)
- 3B.4: Test marker color boost increases saturation
- 3B.5: Test book scan splits two-page spread into 2 images
- 3B.5: Test dewarp produces rectangular output

Run tests. ALL must pass.
Only then update PROGRESS.md. Commit.

Read FRONTEND_MVP.md section 4 → Phase 3, deliverables 1-5.
---

VERIFY AFTER:
  [ ] All tests pass (11+ cases)
  [ ] Classifier detects all document types
  [ ] Each smart mode produces correct output
  [ ] PROGRESS.md tasks 3B.1-3B.5 checked ONLY after tests pass


================================================================================
SESSION 27 — Frontend Phase 3: Annotations + Redaction
================================================================================
DATE: ___________  STATUS: [ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 3B.6 through 3B.9.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN tests
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 3B.6: Signature tool (draw, save, place on PDF).
- 3B.7: Annotations (text, highlighter, redaction, undo/redo 30 steps).
- 3B.8: Destructive redaction (pixels destroyed, OCR boxes wiped).
- 3B.9: Image cleanup (denoise, sharpen, fix lighting).

TESTS TO WRITE:
- 3B.6: UI test — signature canvas draws with finger input
- 3B.6: Test save signature → retrievable
- 3B.6: Test place signature on PDF page
- 3B.7: UI test — text annotation renders on document
- 3B.7: UI test — highlighter draws on document
- 3B.7: Test undo removes last annotation
- 3B.7: Test redo restores undone annotation
- 3B.7: Test undo stack max is 30
- 3B.8: Test redaction replaces pixels with black (verify raw data)
- 3B.8: Test OCR bounding boxes wiped in redacted area
- 3B.8: Test redaction is irreversible (original data gone)
- 3B.9: Test denoise filter produces smoother output
- 3B.9: Test sharpen filter produces sharper output
- 3B.9: Test filters are non-destructive (stored as flags)

Run tests. ALL must pass.
Only then update PROGRESS.md. Commit.

Read FRONTEND_MVP.md section 4 → Phase 3, deliverables 6-9.
---

VERIFY AFTER:
  [ ] All tests pass (14+ cases)
  [ ] Redaction verified as truly destructive
  [ ] Undo/redo stack tested
  [ ] PROGRESS.md tasks 3B.6-3B.9 checked ONLY after tests pass


================================================================================
SESSION 28 — Frontend Phase 3: Ads + Onboarding + Store
================================================================================
DATE: ___________  STATUS: [ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 3B.10 through 3B.13.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN tests
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 3B.10: AdMob — lazy init (NOT in Application.onCreate). UMP
  consent for EU. Interstitial after every 5th export. Rewarded ad
  for batch enhancement. NO banner on camera. Freq cap: 1/3min.
- 3B.11: In-app review API — trigger: 3+ exports AND 3+ days AND
  no prompt in 90 days.
- 3B.12: Onboarding — 3 screens, skippable, never shown again.
- 3B.13: Play Store prep — listing copy, screenshots, feature graphic.

TESTS TO WRITE:
- 3B.10: Test AdMob NOT initialized in Application.onCreate
- 3B.10: Test interstitial shown after 5th export (not before)
- 3B.10: Test frequency cap (no interstitial within 3 minutes)
- 3B.10: Test no banner on camera screen
- 3B.10: Test UMP consent shown for EU locale
- 3B.11: Test review prompt conditions (all 3 must be true)
- 3B.11: Test review not prompted again within 90 days
- 3B.12: UI test — onboarding 3 screens render
- 3B.12: UI test — skip button works
- 3B.12: Test onboarding never shown after first completion
- 3B.12: Test onboarding requests camera permission on last screen
- 3B.13: Verify listing copy file exists
- 3B.13: Verify screenshot dimensions correct

Run tests. ALL must pass.
Only then update PROGRESS.md.
Check if Phase 3B is fully complete.
Commit: "feat(android): Phase 3 AI modes & monetization with tests"

Read FRONTEND_MVP.md section 4 → Phase 3 and DESIGN_SYSTEM.md 2.13-2.14.
---

VERIFY AFTER:
  [ ] All tests pass (13+ cases)
  [ ] Ad behavior tested (timing, frequency, placement)
  [ ] Onboarding shows once then disappears
  [ ] ALL Phase 3B tasks in PROGRESS.md are checked
  [ ] PROGRESS.md tasks checked ONLY after tests pass


================================================================================
SESSION 29 — Intelligence Layer v1: Docker + Health + Classify
================================================================================
DATE: ___________  STATUS: [ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 3C.1 through 3C.4.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN: cd intelligence && pytest tests/ -v
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

ARCHITECTURE REMINDER FOR THIS SESSION:
- Python service runs on port 8100 (Docker container, internal only)
- Python is NEVER exposed to the internet — Caddy does NOT proxy port 8100
- Android NEVER calls Python directly — all AI calls go Android → Go → Python
- This session builds Python in isolation; Go integration happens in Session 36
- Redis queue (for Session 30): scanvault:intelligence:tasks
- Redis result channel (for Session 30): scanvault:intelligence:results

- 3C.1: Finalize FastAPI app — health endpoint working in Docker.
- 3C.2: Classification endpoint (heuristic + ML model loading).
- 3C.3: Enhanced OCR endpoint (PaddleOCR, single page).
- 3C.4: Vision enhance endpoint (denoise, sharpen, balance pipeline).

TESTS TO WRITE:
- 3C.1: Test /health returns {"status": "healthy"} with 200
- 3C.1: Test /health includes version and uptime
- 3C.1: Test app starts without errors (import test)
- 3C.2: Test classify with document image → correct type
- 3C.2: Test classify with invalid image → graceful error, not crash
- 3C.2: Test classify returns confidence score 0-1
- 3C.2: Test model lazy loads on first request
- 3C.3: Test OCR extracts text from clear image
- 3C.3: Test OCR returns empty for image without text
- 3C.3: Test OCR handles corrupted image → error response
- 3C.3: Test OCR handles large image (50MB) → rejected or handled
- 3C.4: Test denoise produces different output than input
- 3C.4: Test sharpen produces different output than input
- 3C.4: Test enhance handles corrupted image → error response
- 3C.4: Test enhance with timeout → proper timeout error

Run: cd intelligence && pytest tests/ -v --tb=short
ALL tests must pass. Only then update PROGRESS.md. Commit.

Read INTELLIGENCE_LAYER.md section 9 → Phase 1.
---

VERIFY AFTER:
  [ ] pytest passes ALL tests (15+ cases)
  [ ] Health endpoint tested
  [ ] Classify tested: happy path, errors, lazy loading
  [ ] OCR tested: happy path, errors, large input
  [ ] Vision enhance tested: happy path, errors, timeout
  [ ] PROGRESS.md tasks 3C.1-3C.4 checked ONLY after tests pass


================================================================================
SESSION 30 — Intelligence Layer v1: Queue + R2 + Docker Compose
================================================================================
DATE: ___________  STATUS: [ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 3C.5 through 3C.8.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN: cd intelligence && pytest tests/ -v
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 3C.5: Redis queue integration (ARQ worker consuming tasks).
- 3C.6: R2 integration (download input, upload output).
- 3C.7: Docker compose working (API + worker + Redis).
- 3C.8: Tests passing in CI.

TESTS TO WRITE:
- 3C.5: Test task enqueue → worker picks it up
- 3C.5: Test task status transitions (pending → processing → completed)
- 3C.5: Test task failure → status is "failed" with error message
- 3C.5: Test task timeout → marked as timed out
- 3C.6: Test R2 download (mock S3 client)
- 3C.6: Test R2 upload (mock S3 client)
- 3C.6: Test R2 handles missing key → proper error
- 3C.7: Test docker-compose.yml is valid YAML
- 3C.7: Test all services defined (api, worker, redis)
- 3C.8: Verify all tests pass: pytest tests/ -v
- 3C.8: Verify ruff check passes
- 3C.8: Verify ruff format --check passes
- 3C.8: Verify pyright passes

Run: cd intelligence && pytest tests/ -v && ruff check src/ tests/ && ruff format --check src/ tests/
ALL must pass. Only then update PROGRESS.md.
Check if Phase 3C is fully complete.
Commit: "feat(intelligence): Phase 1 complete with full tests"

Read INTELLIGENCE_LAYER.md section 9 → Phase 1.
---

VERIFY AFTER:
  [ ] pytest passes ALL tests
  [ ] ruff lint and format pass
  [ ] Queue lifecycle tested
  [ ] R2 integration tested
  [ ] ALL Phase 3C tasks in PROGRESS.md are checked
  [ ] PROGRESS.md tasks checked ONLY after tests pass


================================================================================
SESSION 31 — Backend Phase 4: Observability (Part 1)
================================================================================
DATE: ___________  STATUS: [ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 4A.1 through 4A.4.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN: go test -race ./...
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 4A.1: Prometheus metrics exposition (/metrics endpoint).
- 4A.2: Grafana dashboards (JSON provisioning files).
- 4A.3: Structured error handling + Sentry integration.
- 4A.4: Deep health checks (DB, Redis, R2, intelligence service).

TESTS TO WRITE:
- 4A.1: Test /metrics returns Prometheus format
- 4A.1: Test request_count, request_duration metrics recorded
- 4A.1: Test metrics increment on requests
- 4A.2: Verify Grafana JSON files are valid JSON
- 4A.2: Verify dashboards reference correct Prometheus queries
- 4A.3: Test error handler returns structured JSON errors
- 4A.3: Test 500 errors reported to Sentry (mock client)
- 4A.3: Test 4xx errors NOT reported to Sentry
- 4A.4: Test deep health check passes when all services up
- 4A.4: Test deep health check fails when DB down
- 4A.4: Test deep health check fails when Redis down
- 4A.4: Test response includes status of each component

Run: cd backend && go test -race ./...
ALL tests must pass. Only then update PROGRESS.md. Commit.

Read BACKEND_MVP.md section 6 → Phase 4.
---

VERIFY AFTER:
  [ ] go test -race ./... passes ALL tests (12+ cases)
  [ ] Metrics endpoint returns Prometheus format
  [ ] Error handling tested
  [ ] Deep health checks tested
  [ ] PROGRESS.md tasks 4A.1-4A.4 checked ONLY after tests pass


================================================================================
SESSION 32 — Backend Phase 4: Performance + Backup
================================================================================
DATE: ___________  STATUS: [ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 4A.5 through 4A.9.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN: go test -race ./...
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 4A.5: Database performance tuning (indexes, EXPLAIN ANALYZE).
- 4A.6: Caching layer (in-process LRU for hot queries).
- 4A.7: Backup strategy (nightly pg_dump → age encrypt → R2).
- 4A.8: Load testing (k6/vegeta script, 500 concurrent users).
- 4A.9: Runbook (docs/RUNBOOK.md).

TESTS TO WRITE:
- 4A.5: Test key queries use indexes (EXPLAIN shows Index Scan)
- 4A.5: Test no sequential scans on large tables
- 4A.6: Test cache hit returns same result faster
- 4A.6: Test cache invalidated on write
- 4A.6: Test cache size bounded (LRU eviction)
- 4A.7: Test backup script runs without error (dry-run mode)
- 4A.7: Test backup output is encrypted (not readable as SQL)
- 4A.8: Test load test script runs and produces report
- 4A.9: Verify RUNBOOK.md exists with required sections

Run: cd backend && go test -race ./...
ALL tests must pass. Only then update PROGRESS.md.
Check if Phase 4A is fully complete.
Commit: "feat(backend): Phase 4 observability & performance with tests"

Read BACKEND_MVP.md section 6 → Phase 4.
---

VERIFY AFTER:
  [ ] go test -race ./... passes ALL tests
  [ ] Query performance validated
  [ ] Cache tested: hit, miss, eviction
  [ ] Backup script tested in dry-run
  [ ] ALL Phase 4A tasks in PROGRESS.md are checked
  [ ] PROGRESS.md tasks checked ONLY after tests pass


================================================================================
SESSION 33 — Frontend Phase 4: Account + Crypto
================================================================================
DATE: ___________  STATUS: [ ] DONE

⚠️  PRE-FLIGHT GATE — verify before writing any code:
    curl https://api-staging.scanvault.app/v1/health
    Must return HTTP 200. If not, backend Phase 1-3 is not deployed.
    DO NOT start this session until the gate passes.

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 4B.1 through 4B.4.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN tests
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

THIS SESSION IS THE FIRST TIME ANDROID CALLS THE GO BACKEND.

API details (read docs/FRONTEND_MVP.md Phase 4 → "API Contract" for full spec):
- Base URL configured via BuildConfig.API_BASE_URL (staging build → api-staging.scanvault.app/v1)
- Auth: Paseto token in Authorization header for all endpoints except POST /accounts and POST /sessions
- Error format: {"error": "message"} for 4xx/5xx
- 4B.1 endpoints: POST /accounts (create), POST /sessions (login), PUT /sessions (refresh)
- 4B.3 endpoints: POST /vault/blobs (upload), GET /vault/blobs/{id} (download), GET /vault/manifest, DELETE /vault/blobs/{id}
- Ktor Client with OkHttp engine — do NOT use Retrofit, Volley, or HttpURLConnection
- See android/core/network/NetworkModule.kt for the Ktor client setup stub

- 4B.1: Account screen (signup, login, password strength indicator).
- 4B.2: Client-side E2E crypto (libsodium, K_master, K_encrypt, HKDF).
- 4B.3: Sync engine (WorkManager, upload/download/delete/rename).
- 4B.4: Conflict resolution UI (server vs local, user picks).

TESTS TO WRITE:
- 4B.1: UI test — signup form renders all fields
- 4B.1: UI test — password strength indicator updates
- 4B.1: UI test — login form validates email format
- 4B.1: Test form submission calls POST /accounts (mock Ktor client)
- 4B.1: Test login calls POST /sessions (mock Ktor client)
- 4B.2: Test key derivation produces consistent output
- 4B.2: Test encrypt → decrypt → same plaintext
- 4B.2: Test different keys produce different ciphertext
- 4B.2: Test encrypted data is not readable as plaintext
- 4B.2: Test key rotation re-wraps correctly
- 4B.3: Test sync schedules upload for new document
- 4B.3: Test sync retries on network failure (exponential backoff)
- 4B.3: Test sync handles offline gracefully
- 4B.3: Test sync upload + download lifecycle (POST /vault/blobs → GET /vault/blobs/{id})
- 4B.4: UI test — conflict dialog shows server vs local versions
- 4B.4: Test user can pick server or local version
- 4B.4: Test conflict resolution persists choice

Run tests. ALL must pass.
Only then update PROGRESS.md. Commit.

Read FRONTEND_MVP.md section 4 → Phase 4 (full section, including "API Contract" and ":core:network Module" subsections added in the interconnection update).
---

VERIFY AFTER:
  [ ] Staging gate check passed before coding
  [ ] All tests pass (17+ cases)
  [ ] Ktor client used (not Retrofit/OkHttp directly)
  [ ] API base URL comes from BuildConfig, not hardcoded
  [ ] E2E crypto: encrypt/decrypt round-trip verified
  [ ] Sync engine tested: online, offline, retry
  [ ] Conflict UI tested
  [ ] PROGRESS.md tasks 4B.1-4B.4 checked ONLY after tests pass


================================================================================
SESSION 34 — Frontend Phase 4: Sync Status + Security + Account Mgmt
================================================================================
DATE: ___________  STATUS: [ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 4B.5 through 4B.10.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN tests
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

API reminders for this session:
- DELETE /accounts/me → delete account (Go handler: internal/accounts)
- All endpoints require: Authorization: Bearer <paseto_token>
- 503 from server → retry with exponential backoff (do NOT crash)
- Certificate pinning: pin against api.scanvault.app SHA-256 — see docs/FRONTEND_MVP.md
  Phase 4 → ":core:network Module" for the exact Ktor OkHttp CertificatePinner snippet
- Intelligence endpoints added in this phase: POST /intelligence/tasks, GET /intelligence/tasks/{id}
  (these require the Go backend Phase 4C to be done — stub the calls if 4C isn't done yet)

- 4B.5: Sync status in library cards (cloud-done, uploading, pending, local-only).
- 4B.6: Certificate pinning (Ktor + OkHttp).
- 4B.7: Tamper check (signature verification).
- 4B.8: Account management (change password, delete, logout, export).
- 4B.9: Ktor client setup (HTTP/2, retry, offline queue).
- 4B.10: Integration test (account → scan → sync → second device → see docs).

TESTS TO WRITE:
- 4B.5: UI test — each sync status icon renders correctly
- 4B.5: Test status transitions (local → pending → uploading → cloud)
- 4B.6: Test connection with valid cert → succeeds
- 4B.6: Test connection with wrong cert → fails (pinning works)
- 4B.7: Test tamper check passes for unmodified APK
- 4B.7: Test tamper check detects modification
- 4B.8: UI test — change password flow
- 4B.8: Test password change re-wraps encryption key
- 4B.8: UI test — delete account flow with confirmation (DELETE /accounts/me)
- 4B.8: Test data export produces encrypted ZIP
- 4B.9: Test Ktor client retries on 503
- 4B.9: Test Ktor client queues requests when offline
- 4B.10: Full E2E integration test:
  create account → scan document → sync to server → verify on "second device"

Run tests. ALL must pass.
Only then update PROGRESS.md.
Check if Phase 4B is fully complete.
Commit: "feat(android): Phase 4 cloud sync & E2E crypto with tests"

Read FRONTEND_MVP.md section 4 → Phase 4 (full section including "Intelligence Feature — What Android Does in Phase 4C").
---

VERIFY AFTER:
  [ ] All tests pass (13+ cases)
  [ ] Ktor cert pinning uses sha256/ format (not hex)
  [ ] DELETE /accounts/me tested
  [ ] Full sync integration tested
  [ ] ALL Phase 4B tasks in PROGRESS.md are checked
  [ ] PROGRESS.md tasks checked ONLY after tests pass


================================================================================
SESSION 35 — Intelligence Layer v2: Layout + Extraction
================================================================================
DATE: ___________  STATUS: [ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 4C.1 through 4C.3.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN: cd intelligence && pytest tests/ -v
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 4C.1: Layout analysis (headers, paragraphs, tables, figures).
- 4C.2: Field extraction (receipts, invoices, IDs).
- 4C.3: Super-resolution (Real-ESRGAN x2 model, CPU inference).

TESTS TO WRITE:
- 4C.1: Test layout analysis detects headers in document image
- 4C.1: Test layout analysis detects table structure
- 4C.1: Test returns structured blocks with types
- 4C.1: Test handles image without clear layout gracefully
- 4C.2: Test receipt extraction: total, merchant, date
- 4C.2: Test invoice extraction: invoice number, line items
- 4C.2: Test ID extraction: name, number
- 4C.2: Test extraction with poor quality image → partial results
- 4C.2: Test extraction with non-matching document type → empty
- 4C.3: Test super-resolution output is 2x input dimensions
- 4C.3: Test super-resolution produces higher quality output
- 4C.3: Test super-resolution handles small image
- 4C.3: Test super-resolution handles corrupted image → error
- 4C.3: Test super-resolution with timeout → proper error

Run: cd intelligence && pytest tests/ -v --tb=short
ALL tests must pass. Only then update PROGRESS.md. Commit.

Read INTELLIGENCE_LAYER.md section 9 → Phase 2.
---

VERIFY AFTER:
  [ ] pytest passes ALL tests (14+ cases)
  [ ] Layout analysis returns structured blocks
  [ ] Field extraction works for receipts, invoices, IDs
  [ ] Super-resolution produces 2x output
  [ ] PROGRESS.md tasks 4C.1-4C.3 checked ONLY after tests pass


================================================================================
SESSION 36 — Intelligence Layer v2: Go Integration
================================================================================
DATE: ___________  STATUS: [ ] DONE

⚠️  PRE-FLIGHT GATE — verify before writing any code:
    docker compose up intelligence -d
    curl http://localhost:8100/health  → must return {"status": "healthy"}
    redis-cli ping                     → must return PONG
    DO NOT start until both pass.

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 4C.4 through 4C.7.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN: go test -race ./... (for Go) AND pytest tests/ -v (for Python)
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

THIS SESSION IS THE FIRST TIME GO CALLS PYTHON.

Key integration constants (use these EXACTLY — do NOT invent alternatives):
- Python service URL (from Go): http://localhost:8100
- Redis queue name: scanvault:intelligence:tasks
- Redis result channel: scanvault:intelligence:results
- R2 processing prefix: processing/<account_uuid>/<task_uuid>/  (1-hour TTL)
- Python port 8100 is INTERNAL ONLY — Caddy does NOT proxy it
- Migration number: 0004 (intelligence_tasks table — schema in INTELLIGENCE_LAYER.md §2.1)

internal/intelligence/ package structure (create exactly this):
  handler.go    → HTTP handlers: submit job, poll status, get result, cancel
  service.go    → Business logic: validate, enqueue, quota check
  queue.go      → Redis publisher (enqueue tasks to scanvault:intelligence:tasks)
  client.go     → HTTP client to Python :8100 (sync classify path)
  models.go     → Task types, status enums, request/response structs
  repository.go → sqlc queries for intelligence_tasks table

Rate limits on intelligence endpoints:
  POST /v1/intelligence/classify → 20 requests/min per account (sync, lightweight)
  POST /v1/intelligence/tasks    → 100 requests/hour per account (async, heavy)

- 4C.4: Go internal/intelligence/ package — HTTP client to Python + Redis publisher.
- 4C.5: Migration 0004 for intelligence_tasks table.
- 4C.6: Go API endpoints: POST /v1/intelligence/classify (sync), POST /v1/intelligence/tasks (async), GET /v1/intelligence/tasks/{id} (poll), DELETE /v1/intelligence/tasks/{id} (cancel).
- 4C.7: Prometheus metrics for intelligence services.

TESTS TO WRITE:
- 4C.4: Test Go HTTP client calls Python :8100/classify (mock httptest server)
- 4C.4: Test Go Redis publisher enqueues to scanvault:intelligence:tasks (mock Redis)
- 4C.4: Test Go client handles Python service timeout (returns 503, not panic)
- 4C.4: Test Go client handles Python service completely down (returns 503, not panic)
- 4C.5: Integration test — migration 0004 creates intelligence_tasks with all indexes
- 4C.5: Test table constraints (invalid task_type → error, invalid status → error)
- 4C.6: Test POST /v1/intelligence/classify → auth required → proxies to Python mock
- 4C.6: Test POST /v1/intelligence/tasks → auth required → creates async task → 202
- 4C.6: Test GET /v1/intelligence/tasks/{id} → returns correct status
- 4C.6: Test GET /v1/intelligence/tasks/{id} → other account's task → 404
- 4C.6: Test DELETE /v1/intelligence/tasks/{id} → cancels pending task
- 4C.6: Test rate limiting on classify (21st request → 429)
- 4C.6: Test no auth → 401 on all 4 endpoints
- 4C.7: Test intelligence_requests_total metric increments
- 4C.7: Test intelligence_queue_depth metric updates

Run both: cd backend && go test -race ./... AND cd intelligence && pytest tests/ -v
ALL must pass. Only then update PROGRESS.md.
Check if Phase 4C is fully complete.
Commit: "feat(intelligence): Phase 2 + Go integration with tests"

Read INTELLIGENCE_LAYER.md sections 2-4 AND BACKEND_MVP.md §5 intelligence proxy section.
---

VERIFY AFTER:
  [ ] Pre-flight gate passed (Python :8100 and Redis healthy)
  [ ] go test -race ./... AND pytest pass ALL tests (15+ cases)
  [ ] internal/intelligence/ has exactly 6 files (handler, service, queue, client, models, repository)
  [ ] Python URL is http://localhost:8100 (not hardcoded in tests — use config)
  [ ] Redis queue name is scanvault:intelligence:tasks (exact string)
  [ ] Go returns 503 when Python is down (verified by test)
  [ ] Migration is numbered 0004
  [ ] ALL Phase 4C tasks in PROGRESS.md are checked
  [ ] PROGRESS.md tasks checked ONLY after tests pass


================================================================================
SESSION 37 — Phase 5: Baseline Profiles + R8 + Dark Mode
================================================================================
DATE: ___________  STATUS: [ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 5.1 through 5.3.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN tests
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 5.1: Macrobenchmark → generate baseline profile → include in APK.
- 5.2: R8 full mode — verify nothing breaks. Tree-shake OpenCV.
  Target APK <25MB.
- 5.3: Dark mode — full Material 3 dark theme. Manual toggle.

TESTS TO WRITE:
- 5.1: Run Macrobenchmark — cold start, library scroll, camera launch
- 5.1: Verify baseline profile generated and included
- 5.2: Test release build with R8 → no crashes (run all existing tests on release build)
- 5.2: Verify APK size < 25MB
- 5.2: Test Hilt injection works with R8
- 5.2: Test kotlinx.serialization works with R8
- 5.3: UI test — every screen renders correctly in dark mode
- 5.3: UI test — every screen renders correctly in light mode
- 5.3: Test toggle switches between modes
- 5.3: Test "system" setting follows device theme

Run tests. ALL must pass.
Only then update PROGRESS.md. Commit.

Read FRONTEND_MVP.md section 4 → Phase 5 and DESIGN_SYSTEM.md 2.10.
---

VERIFY AFTER:
  [ ] All tests pass
  [ ] Baseline profile ships with APK
  [ ] R8 doesn't break anything (verified by running full test suite)
  [ ] APK under 25MB
  [ ] Dark mode correct on every screen
  [ ] PROGRESS.md tasks 5.1-5.3 checked ONLY after tests pass


================================================================================
SESSION 38 — Phase 5: Accessibility + Localization
================================================================================
DATE: ___________  STATUS: [ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 5.4 through 5.5.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN tests
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 5.4: Accessibility pass (contentDescription, TalkBack, 48dp targets,
  contrast ratios).
- 5.5: Localization (EN, HI, NE, ES, PT, AR, FR, DE, ID — RTL tested).

TESTS TO WRITE:
- 5.4: Test every interactive element has contentDescription
- 5.4: Test every touch target >= 48dp
- 5.4: Test contrast ratios meet WCAG AA (4.5:1 text, 3:1 large text)
- 5.4: Test TalkBack can navigate entire app flow
- 5.5: Test all strings have translations in all 9 locales
- 5.5: Test no hardcoded strings in Compose code
- 5.5: Test RTL layout correct for Arabic locale
- 5.5: Test date/number formatting per locale

Run tests. ALL must pass.
Only then update PROGRESS.md. Commit.
---

VERIFY AFTER:
  [ ] All tests pass
  [ ] Accessibility audit clean
  [ ] All 9 locales have complete translations
  [ ] RTL layout verified for Arabic
  [ ] PROGRESS.md tasks 5.4-5.5 checked ONLY after tests pass


================================================================================
SESSION 39 — Phase 5: Widgets + Quick Settings + Share
================================================================================
DATE: ___________  STATUS: [ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 5.6 through 5.8.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN tests
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 5.6: Widgets (Glance: "Scan now" + "Recent scans").
- 5.7: Quick Settings tile.
- 5.8: Share extension (receive images from other apps).

TESTS TO WRITE:
- 5.6: Test "Scan now" widget renders
- 5.6: Test "Scan now" widget launches camera on tap
- 5.6: Test "Recent scans" widget shows thumbnails
- 5.6: Test widgets update when new scan added
- 5.7: Test Quick Settings tile renders with correct icon
- 5.7: Test tile tap launches camera
- 5.8: Test receiving shared image from gallery
- 5.8: Test receiving shared PDF
- 5.8: Test receiving multiple shared images
- 5.8: Test receiving invalid file type → graceful handling

Run tests. ALL must pass.
Only then update PROGRESS.md. Commit.
---

VERIFY AFTER:
  [ ] All tests pass (10+ cases)
  [ ] Widgets render and respond to taps
  [ ] Quick Settings tile works
  [ ] Share extension handles images and PDFs
  [ ] PROGRESS.md tasks 5.6-5.8 checked ONLY after tests pass


================================================================================
SESSION 40 — Phase 5: Performance + Error Reporting
================================================================================
DATE: ___________  STATUS: [ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 5.9 through 5.11.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — write tests (see below)
3. RUN tests
4. PASS — all must pass before continuing
5. UPDATE PROGRESS.md only after tests pass

- 5.9: Performance pass (cold start <500ms, library 60fps@1000 docs,
  memory <150MB).
- 5.10: Error reporting (custom crash handler, encrypted logs).
- 5.11: Backup/restore (local encrypted ZIP).

TESTS TO WRITE:
- 5.9: Benchmark cold start time (must be <500ms)
- 5.9: Benchmark library scroll with 1000 documents (must be 60fps)
- 5.9: Test memory usage under <150MB during normal operation
- 5.10: Test crash handler captures exception details
- 5.10: Test crash logs are encrypted (not readable as plaintext)
- 5.10: Test crash handler doesn't crash itself (meta-test)
- 5.11: Test backup creates encrypted ZIP
- 5.11: Test restore from backup → all data recovered
- 5.11: Test backup → restore round-trip (data matches)
- 5.11: Test restore with wrong password → fails gracefully
- 5.11: Test backup handles 0 documents → valid empty backup

Run tests. ALL must pass.
Only then update PROGRESS.md. Commit.
---

VERIFY AFTER:
  [ ] All tests pass (11+ cases)
  [ ] Performance benchmarks within targets
  [ ] Error reporting tested
  [ ] Backup/restore round-trip tested
  [ ] PROGRESS.md tasks 5.9-5.11 checked ONLY after tests pass


================================================================================
SESSION 41 — Phase 5: Backend Production + Legal
================================================================================
DATE: ___________  STATUS: [ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 5.12 through 5.13.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the code
2. TEST — validate (see below)
3. RUN validation
4. PASS — all checks pass
5. UPDATE PROGRESS.md only after validation passes

- 5.12: Backend production environment — separate VPS, TLS 1.3,
  pen test findings resolved.
- 5.13: Privacy policy + Terms of Service.

TESTS TO WRITE:
- 5.12: Verify Caddyfile has TLS 1.3 minimum
- 5.12: Verify HSTS with preload configured
- 5.12: Verify no debug endpoints exposed in production config
- 5.12: Verify prod environment vars are different from staging
- 5.12: Verify pen test checklist items addressed
- 5.13: Verify privacy policy file exists and covers required sections
  (data collection, storage, sharing, deletion, children's privacy)
- 5.13: Verify ToS file exists
- 5.13: Verify privacy policy URL accessible
- 5.13: Verify data safety section matches Play Console declarations

ALL checks must pass. Only then update PROGRESS.md. Commit.

Read BACKEND_MVP.md section 6 → Phase 5.
---

VERIFY AFTER:
  [ ] All validation checks pass
  [ ] Production config has TLS 1.3
  [ ] Privacy policy covers all required sections
  [ ] PROGRESS.md tasks 5.12-5.13 checked ONLY after tests pass


================================================================================
SESSION 42 — Phase 5: Release Workflows
================================================================================
DATE: ___________  STATUS: [ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete tasks 5.14 through 5.16.

FOLLOW THE TEST-GATE RULE FOR EACH TASK:
1. BUILD the workflow YAML
2. TEST — validate (see below)
3. RUN validation
4. PASS — all checks pass
5. UPDATE PROGRESS.md only after validation passes

- 5.14: android-release.yml (tag → Fastlane → Play Console →
  staged rollout → GitHub release → Sentry marker).
- 5.15: backend-deploy-production.yml (tag → prod VPS → manual
  approval → auto-rollback → Sentry marker).
- 5.16: ota-config-push.yml (validate JSON → upload R2 → purge cache).

TESTS TO WRITE:
- Verify each YAML is valid
- Verify android-release.yml triggers on v*.*.* tags
- Verify android-release.yml has Fastlane steps
- Verify android-release.yml cleans secrets after use
- Verify backend-deploy-production.yml has manual approval gate
- Verify backend-deploy-production.yml has rollback on failure
- Verify ota-config-push.yml validates JSON schemas
- Verify ota-config-push.yml has min_app_version check
- Verify all secrets referenced via ${{ secrets.* }}
- Verify no hardcoded credentials

Write validation script. Run it. ALL checks must pass.
Only then update PROGRESS.md. Commit.

Read DEVOPS_AUTOMATION.md sections 5-7.
---

VERIFY AFTER:
  [ ] Validation script passes all checks
  [ ] android-release.yml has correct Fastlane flow
  [ ] backend-deploy-production.yml has rollback
  [ ] OTA push validates and uploads
  [ ] PROGRESS.md tasks 5.14-5.16 checked ONLY after tests pass


================================================================================
SESSION 43 — Phase 5: Play Store Submission
================================================================================
DATE: ___________  STATUS: [ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete task 5.17.

FOLLOW THE TEST-GATE RULE:
1. BUILD the checklist document
2. TEST — validate completeness (see below)
3. RUN validation
4. PASS — all checks pass
5. UPDATE PROGRESS.md only after validation passes

- 5.17: Play Store submission:
  1. Create Play Console listing with all metadata from session 28
  2. Upload AAB to closed testing track
  3. Add 12 testers, start 14-day closed testing
  4. Complete IARC content rating
  5. Fill data safety form
  6. After 14 days: promote to open testing
  7. After verification: staged rollout 10% → 50% → 100%

  Document all steps in docs/PLAY_STORE_CHECKLIST.md.

TESTS TO WRITE:
- Verify PLAY_STORE_CHECKLIST.md exists
- Verify checklist covers all 7 steps
- Verify data safety form answers documented
- Verify target countries listed
- Verify manual vs automated steps clearly marked
- Verify IARC questionnaire answers documented

ALL checks must pass. Only then update PROGRESS.md. Commit.
---

VERIFY AFTER:
  [ ] Validation passes
  [ ] Checklist covers all steps
  [ ] Data safety form answers documented
  [ ] Target countries listed (US, UK, CA, AU, DE, FR)
  [ ] PROGRESS.md task 5.17 checked ONLY after tests pass


================================================================================
SESSION 44 — Phase 5: Post-Launch Monitoring
================================================================================
DATE: ___________  STATUS: [ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Complete task 5.18.

FOLLOW THE TEST-GATE RULE:
1. BUILD the monitoring setup
2. TEST — validate (see below)
3. RUN validation
4. PASS — all checks pass
5. UPDATE PROGRESS.md only after validation passes

- 5.18: Post-launch monitoring:
  - GitHub Action (30 min after release): check Sentry errors,
    Play Console crash rate (<0.5%), backend p99 latency from Grafana.
  - Green → auto-promote rollout 10% → 50%
  - Red → halt rollout + Telegram alert
  - Document daily monitoring in docs/RUNBOOK.md

TESTS TO WRITE:
- Verify monitoring workflow YAML is valid
- Verify workflow has correct schedule/trigger
- Verify Sentry, Play Console, Grafana checks present
- Verify auto-promote logic correct
- Verify halt + alert logic on failure
- Verify RUNBOOK.md has daily monitoring routine
- Verify RUNBOOK.md covers crash rate, reviews, AdMob metrics

ALL checks must pass. Only then update PROGRESS.md.
Verify ALL PROGRESS.md tasks across ALL phases are complete.
Final commit: "feat: ScanVault v1.0.0 — ready for launch"
---

VERIFY AFTER:
  [ ] All validation checks pass
  [ ] Monitoring workflow created
  [ ] Runbook has daily routine
  [ ] ALL PROGRESS.md tasks are [x] checked
  [ ] PROGRESS.md task 5.18 checked ONLY after tests pass


================================================================================
SESSION 45 — LAUNCH DAY
================================================================================
DATE: ___________  STATUS: [ ] DONE

PROMPT:
---
Read CLAUDE.md and PROGRESS.md. Verify every single task is
complete. Run the final acceptance checklist:

1. ALL Phase 0 tasks: checked
2. ALL Phase 1A tasks: checked
3. ALL Phase 1B tasks: checked
4. ALL Phase 1C tasks: checked
5. ALL Phase 2A tasks: checked
6. ALL Phase 2B tasks: checked
7. ALL Phase 3A tasks: checked
8. ALL Phase 3B tasks: checked
9. ALL Phase 3C tasks: checked
10. ALL Phase 4A tasks: checked
11. ALL Phase 4B tasks: checked
12. ALL Phase 4C tasks: checked
13. ALL Phase 5 tasks: checked

FINAL TEST GATE — Run ALL test suites one last time:
- cd backend && go test -race -cover ./...
- cd android && ./gradlew testDebugUnitTest
- cd intelligence && pytest tests/ -v

ALL three must pass with zero failures.

If any task is unchecked OR any test fails, list it.
Otherwise report: "ScanVault is ready to ship."

Then run: ./scripts/release.sh minor

This triggers the full release pipeline via GitHub Actions.
---

VERIFY AFTER:
  [ ] EVERY task in PROGRESS.md is [x]
  [ ] ALL test suites pass (Go + Android + Python)
  [ ] Release tag pushed
  [ ] GitHub Actions running
  [ ] Play Store upload in progress
  [ ] Backend deployed to production
  [ ] Telegram notification received
  [ ] DONE. ScanVault is live.


================================================================================
  END OF PROMPT GUIDE

  Total: 45 sessions, ~130 tasks
  Estimated: 11 weeks for a solo dev with Claude Code

  Every session = 1 copy-paste prompt
  Claude reads only what it needs (~300 lines, not 3000)
  You verify manually after each session
  Progress persists in PROGRESS.md across sessions

  TEST-GATE ENFORCED ON EVERY SESSION:
  No task is marked done until its tests pass.
  No shortcuts. No exceptions. Your app won't crash.
================================================================================
